steps:
  - name: gcr.io/cloud-builders/docker
    args:
      - build
      - --tag=${_IMAGE}
      - .
substitutions:
  _IMAGE: asia-south1-docker.pkg.dev/planproof-ai/planproof/web:latest
images:
  - ${_IMAGE}
